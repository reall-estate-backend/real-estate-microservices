server:
  port: 8070  # Port for the Payment Service

spring:
  application:
    name: payment-service  # Name of the Payment Service
  data:
    mongodb:
      username: timaslilaila
      password: 7RiZRnQZYmJOKt7u
      host: mongodb  # Use the MongoDB service name in Kubernetes
      port: 27017  # Default MongoDB port
      database: payment
      authentication-database: admin

application:
  config:
    users-url: http://user-service-svc:80/api/v1/users  # Use the user-service service name
    offres-url: http://offre-service-svc:80/api/v1/offres  # Use the offre-service service name
